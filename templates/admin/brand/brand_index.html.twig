{% extends 'admin/base_layout.html.twig' %}

{% block body_class 'admin-index-pro' %}

{% block content_title %}{% endblock %}
{% block page_actions %}{% endblock %}
{% block content_header_wrapper %}{% endblock %}

{% block main %}
    <div class="index-container">
        {# HERO HEADER #}
        <div class="index-hero">
            <div class="index-hero-content">
                <div class="index-hero-icon">
                    <i class="fa fa-tag"></i>
                </div>
                <div>
                    <h1 class="index-hero-title">Marques</h1>
                    <p class="index-hero-subtitle">Gérez les marques de vos produits</p>
                </div>
            </div>
            <div class="index-hero-actions">
                <a href="{{ ea_url().setController('App\\Controller\\Admin\\BrandCrudController').setAction('newBrand').generateUrl() }}" class="btn-hero-primary">
                    <i class="fa fa-plus"></i>
                    <span>Nouvelle marque</span>
                </a>
            </div>
        </div>

        {# STATS CARDS #}
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-icon stat-icon-primary">
                    <i class="fa fa-tag"></i>
                </div>
                <div class="stat-content">
                    <div class="stat-value">{{ total_items }}</div>
                    <div class="stat-label">Marques total</div>
                </div>
            </div>

            <div class="stat-card">
                <div class="stat-icon stat-icon-success">
                    <i class="fa fa-box"></i>
                </div>
                <div class="stat-content">
                    <div class="stat-value">{{ brands|reduce((carry, b) => carry + b.products|length, 0) }}</div>
                    <div class="stat-label">Produits associés</div>
                </div>
            </div>

            <div class="stat-card">
                <div class="stat-icon stat-icon-warning">
                    <i class="fa fa-clock"></i>
                </div>
                <div class="stat-content">
                    <div class="stat-value">{{ brands|filter(b => b.createdAt > date('-30 days'))|length }}</div>
                    <div class="stat-label">Ajoutées ce mois</div>
                </div>
            </div>
        </div>

        {# TABLEAU MARQUES #}
        <div class="data-table-card">
            <div class="data-table-header">
                <h2>Liste des marques</h2>
                <div class="data-table-actions">
                    <a href="{{ path('admin_brands_export') }}" class="btn-outline">
                        <i class="fa fa-download"></i>
                        Export
                    </a>
                </div>
            </div>

            <div class="table-responsive">
                <table class="data-table">
                    <thead>
                    <tr>
                        <th width="80">ID</th>
                        <th>Nom</th>
                        <th width="150">Slug</th>
                        <th width="150">Produits</th>
                        <th width="180">Date de création</th>
                        <th width="180" class="text-end">Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for brand in brands %}
                        <tr>
                            <td>
                                <span class="id-badge">#{{ brand.id }}</span>
                            </td>
                            <td>
                                <div class="name-cell">
                                    <div class="name-icon">
                                        <i class="fa fa-tag"></i>
                                    </div>
                                    <div class="name-content">
                                        <div class="name-title">{{ brand.name }}</div>
                                    </div>
                                </div>
                            </td>
                            <td>
                                <code class="slug-badge">{{ brand.slug }}</code>
                            </td>
                            <td>
                                    <span class="count-badge">
                                        <i class="fa fa-box"></i>
                                        {{ brand.products|length }} produit{{ brand.products|length > 1 ? 's' : '' }}
                                    </span>
                            </td>
                            <td>
                                <span class="date-badge">{{ brand.createdAt|date('d/m/Y à H:i') }}</span>
                            </td>
                            <td class="text-end">
                                <div class="action-buttons">
                                    <a href="{{ ea_url().setController('App\\Controller\\Admin\\BrandCrudController').setAction('editBrand').setEntityId(brand.id).generateUrl() }}" class="btn-action btn-action-edit" title="Modifier">
                                        <i class="fa fa-edit"></i>
                                    </a>
                                    <form method="post" action="{{ path('admin_brand_delete', {id: brand.id}) }}" style="display: inline;" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette marque ?');">
                                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
                                        <button type="submit" class="btn-action btn-action-delete" title="Supprimer">
                                            <i class="fa fa-trash"></i>
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="6" class="text-center py-5">
                                <div class="empty-state">
                                    <i class="fa fa-tags"></i>
                                    <h3>Aucune marque</h3>
                                    <p>Commencez par créer votre première marque</p>
                                    <a href="{{ ea_url().setController('App\\Controller\\Admin\\BrandCrudController').setAction('newBrand').generateUrl() }}" class="btn-hero-primary mt-3">
                                        <i class="fa fa-plus"></i>
                                        Créer une marque
                                    </a>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>

            {# PAGINATION #}
            {% if total_pages > 1 %}
                <div class="pagination-wrapper">
                    <div class="pagination-info">
                        Affichage de {{ (current_page - 1) * per_page + 1 }} à {{ min(current_page * per_page, total_items) }} sur {{ total_items }} résultats
                    </div>
                    <nav class="pagination-nav">
                        <ul class="pagination">
                            {% if current_page > 1 %}
                                <li class="page-item">
                                    <a href="?page={{ current_page - 1 }}" class="page-link">
                                        <i class="fa fa-chevron-left"></i>
                                    </a>
                                </li>
                            {% endif %}

                            {% for page in 1..total_pages %}
                                {% if page == current_page %}
                                    <li class="page-item active">
                                        <span class="page-link">{{ page }}</span>
                                    </li>
                                {% elseif page == 1 or page == total_pages or (page >= current_page - 2 and page <= current_page + 2) %}
                                    <li class="page-item">
                                        <a href="?page={{ page }}" class="page-link">{{ page }}</a>
                                    </li>
                                {% elseif page == current_page - 3 or page == current_page + 3 %}
                                    <li class="page-item disabled">
                                        <span class="page-link">...</span>
                                    </li>
                                {% endif %}
                            {% endfor %}

                            {% if current_page < total_pages %}
                                <li class="page-item">
                                    <a href="?page={{ current_page + 1 }}" class="page-link">
                                        <i class="fa fa-chevron-right"></i>
                                    </a>
                                </li>
                            {% endif %}
                        </ul>
                    </nav>
                </div>
            {% endif %}
        </div>
    </div>
{% endblock %}
