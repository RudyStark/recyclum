<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Choisir ma date d'enlèvement - Recyclum</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #f6f8fa 0%, #e9ecef 100%);
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }
        .client-card {
            max-width: 1000px;
            margin: 3rem auto;
            background: white;
            border-radius: 16px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        .client-card-header {
            background: linear-gradient(135deg, #16C669 0%, #12a356 100%);
            color: white;
            padding: 2.5rem;
            text-align: center;
        }
        .client-card-header h1 {
            font-size: 2rem;
            font-weight: 900;
            margin: 0 0 0.5rem 0;
        }
        .client-card-body {
            padding: 2.5rem;
        }
        .info-box {
            background: linear-gradient(135deg, rgba(22, 198, 105, 0.1) 0%, rgba(22, 198, 105, 0.05) 100%);
            border: 2px solid rgba(22, 198, 105, 0.3);
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 2rem;
        }
        .calendar-container {
            background: white;
            border: 2px solid #e6edf2;
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 2rem;
        }
        .calendar-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }
        .calendar-header button {
            background: #16C669;
            color: white;
            border: none;
            border-radius: 8px;
            padding: 0.5rem 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
        }
        .calendar-header button:hover:not(:disabled) {
            background: #12a356;
            transform: translateY(-2px);
        }
        .calendar-header button:disabled {
            background: #d1d5db;
            cursor: not-allowed;
        }
        .calendar-grid {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            gap: 0.5rem;
        }
        .calendar-day-header {
            text-align: center;
            font-weight: 700;
            font-size: 0.875rem;
            color: #6c7783;
            padding: 0.5rem;
        }
        .calendar-day {
            aspect-ratio: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 2px solid #e6edf2;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.2s;
            font-weight: 600;
        }
        .calendar-day:hover:not(.disabled):not(.other-month):not(.weekend) {
            border-color: #16C669;
            background: rgba(22, 198, 105, 0.1);
        }
        .calendar-day.selected {
            background: #16C669;
            color: white;
            border-color: #16C669;
        }
        .calendar-day.disabled {
            background: #f8fafc;
            color: #d1d5db;
            cursor: not-allowed;
        }
        .calendar-day.other-month {
            color: #d1d5db;
            cursor: default;
        }
        .calendar-day.weekend {
            background: #f8fafc;
            color: #9ca3af;
            cursor: not-allowed;
        }
        .time-slots-container {
            display: none;
            margin-top: 2rem;
        }
        .time-slots-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(100px, 1fr));
            gap: 0.75rem;
            margin-top: 1rem;
        }
        .time-slot {
            padding: 0.75rem;
            border: 2px solid #e6edf2;
            border-radius: 8px;
            text-align: center;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
        }
        .time-slot:hover:not(.disabled) {
            border-color: #16C669;
            background: rgba(22, 198, 105, 0.1);
        }
        .time-slot.selected {
            background: #16C669;
            color: white;
            border-color: #16C669;
        }
        .time-slot.disabled {
            background: #f8fafc;
            color: #d1d5db;
            cursor: not-allowed;
        }
        .confirmation-section {
            display: none;
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(59, 130, 246, 0.05) 100%);
            border: 2px solid #3b82f6;
            border-radius: 12px;
            padding: 1.5rem;
            margin-top: 2rem;
        }
        .btn-confirm {
            background: linear-gradient(135deg, #16C669 0%, #12a356 100%);
            color: white;
            border: none;
            border-radius: 8px;
            padding: 1rem 2rem;
            font-weight: 700;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.2s;
            width: 100%;
            margin-top: 1rem;
        }
        .btn-confirm:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(22, 198, 105, 0.3);
        }
        .btn-confirm:disabled {
            background: #d1d5db;
            cursor: not-allowed;
        }
    </style>
</head>
<body>
<div class="client-card">
    <div class="client-card-header">
        <h1>📅 Choisir ma date d'enlèvement</h1>
        <p style="margin: 0; opacity: 0.95;">{{ request.brand|capitalize }} {{ request.model }}</p>
    </div>

    <div class="client-card-body"
         data-controller="client--buyback-calendar"
         data-client--buyback-calendar-token-value="{{ token }}"
         data-client--buyback-calendar-blocked-slots-value="{{ blockedSlots }}">

        <div class="info-box">
            <h3 style="margin: 0 0 0.75rem 0; color: #16C669; font-size: 1.125rem;">
                <i class="fa fa-info-circle"></i> Informations importantes
            </h3>
            <ul style="margin: 0; padding-left: 1.5rem; color: #6c7783;">
                <li>Sélectionnez une date puis un créneau horaire</li>
                <li>Enlèvement gratuit à votre domicile</li>
                <li>Durée estimée : 15-30 minutes</li>
                <li>Paiement {% if request.paymentMethod == 'virement' %}par virement après enlèvement{% else %}en espèces sur place{% endif %}</li>
            </ul>
        </div>

        {# CALENDRIER #}
        <div class="calendar-container">
            <div class="calendar-header">
                <button id="prev-month" type="button">
                    <i class="fa fa-chevron-left"></i>
                </button>
                <h3 id="current-month-year" style="margin: 0; font-size: 1.25rem; font-weight: 800;"></h3>
                <button id="next-month" type="button">
                    <i class="fa fa-chevron-right"></i>
                </button>
            </div>

            <div class="calendar-grid">
                <div class="calendar-day-header">Lun</div>
                <div class="calendar-day-header">Mar</div>
                <div class="calendar-day-header">Mer</div>
                <div class="calendar-day-header">Jeu</div>
                <div class="calendar-day-header">Ven</div>
                <div class="calendar-day-header">Sam</div>
                <div class="calendar-day-header">Dim</div>
            </div>

            <div id="calendar" class="calendar-grid mt-2"></div>
        </div>

        {# CRÉNEAUX HORAIRES #}
        <div id="time-slots-section" class="time-slots-container">
            <h3 style="font-size: 1.125rem; font-weight: 800; margin-bottom: 0.5rem;">
                <i class="fa fa-clock"></i> Choisissez un créneau horaire
            </h3>
            <p style="color: #6c7783; font-size: 0.875rem; margin-bottom: 1rem;">
                Les créneaux grisés ne sont pas disponibles
            </p>
            <div id="time-slots" class="time-slots-grid"></div>
        </div>

        {# CONFIRMATION #}
        <div id="confirmation-section" class="confirmation-section">
            <h3 style="margin: 0 0 1rem 0; font-weight: 800; color: #3b82f6;">
                <i class="fa fa-check-circle"></i> Récapitulatif
            </h3>
            <div style="display: flex; align-items: center; gap: 1rem; margin-bottom: 1rem;">
                <div style="flex: 1;">
                    <div style="color: #6c7783; font-size: 0.875rem;">Date</div>
                    <div id="selected-date-display" style="font-weight: 700; font-size: 1.125rem;"></div>
                </div>
                <div style="flex: 1;">
                    <div style="color: #6c7783; font-size: 0.875rem;">Heure</div>
                    <div id="selected-time-display" style="font-weight: 700; font-size: 1.125rem;"></div>
                </div>
            </div>
            <button id="btn-confirm-appointment" class="btn-confirm" type="button">
                <i class="fa fa-check"></i> Confirmer mon rendez-vous
            </button>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
{{ encore_entry_script_tags('client') }}
</body>
</html>
